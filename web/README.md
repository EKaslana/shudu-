# Web

这里收纳网页端的共用资源，避免根目录散落太多脚本和样式文件。

结构说明：

- `styles/`
  网页端登录页、版本选择页、统计页和对局页共用样式。
- `scripts/`
  网页端入口流程、背景体验、统计面板和访问统计上报脚本。

网页端的正式页面入口仍然保留在仓库根目录与 `play/` 下，这样可以继续兼容本地 Node 服务和 GitHub Pages 的现有路径。
